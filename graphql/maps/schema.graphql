type Query {
  GetReviewsNearby(radius: Int!, lat: Float!, lng: Float!): JSON
  GetLocationsNearby(radius: Int!, lat: Float!, lng: Float!, name: String, category: String): JSON
  GetLocationDetails(id: String!): JSON
}